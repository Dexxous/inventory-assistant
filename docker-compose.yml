services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/prisma/data
    environment:
      - NODE_ENV=production
      - NEXTAUTH_SECRET=inventory-super-secret-key-2024
      - NEXTAUTH_URL=http://localhost:3000
    restart: unless-stopped